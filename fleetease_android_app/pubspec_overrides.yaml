# This file helps certain CI setups and editors understand the repo structure.
# It is safe to ignore locally. It doesn't alter the app; it only clarifies tool context.
environment:
  sdk: ">=3.7.0 <4.0.0"
