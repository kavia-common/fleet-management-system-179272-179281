# Environment example for FleetEase Flutter app

# Data source selection:
# - memory: use in-memory data (non-persistent)
# - sqlite: use local SQLite database (persistent) [default if unset]
DATA_SOURCE=sqlite

# Add other environment variables here as needed.
