name: fleetease_workspace_stub
description: "Root-level Flutter stub to satisfy CI project-root detection. Real app is under fleet-management-system-179272-179281/fleetease_android_app"
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
